// [pengajar-form-en-v1] English version of the teacher registration form.
// Native teachers receive this link directly, so the page must be readable
// without hunting for a language switch first.
import FormPengajar from "../FormPengajar";

export default function JadiPengajarEnPage() {
  return <FormPengajar lang="en" />;
}
